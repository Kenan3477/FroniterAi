# Core ML and Data Science Dependencies
torch>=1.9.0
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
scipy>=1.7.0
matplotlib>=3.4.0
seaborn>=0.11.0

# Database and Storage
sqlalchemy>=1.4.0
sqlite3  # Built-in with Python
psycopg2-binary>=2.9.0  # PostgreSQL support (optional)
redis>=4.0.0  # For Redis storage backend (optional)

# Cloud Storage (optional - install as needed)
boto3>=1.20.0  # AWS S3
azure-storage-blob>=12.8.0  # Azure Blob Storage
google-cloud-storage>=2.0.0  # Google Cloud Storage

# Model Format Conversion
onnx>=1.10.0  # ONNX format support
onnxruntime>=1.10.0  # ONNX inference
# tensorrt>=8.0.0  # TensorRT (requires NVIDIA setup)
# tensorflow>=2.7.0  # TensorFlow (optional)
# onnx-tf>=1.9.0  # ONNX to TensorFlow conversion (optional)

# ML Experiment Tracking (optional)
mlflow>=1.20.0  # MLflow registry integration
wandb>=0.12.0  # Weights & Biases integration

# API and Web Framework (for model serving)
fastapi>=0.70.0
uvicorn>=0.15.0
pydantic>=1.8.0

# Utilities
python-dateutil>=2.8.0
pytz>=2021.3
tqdm>=4.62.0
click>=8.0.0  # CLI support
pyyaml>=6.0
requests>=2.26.0

# Development and Testing
pytest>=6.2.0
pytest-asyncio>=0.18.0
black>=21.0.0  # Code formatting
flake8>=4.0.0  # Linting
mypy>=0.910  # Type checking

# Monitoring and Observability
prometheus-client>=0.12.0  # Metrics collection
structlog>=21.5.0  # Structured logging

# Security
cryptography>=3.4.0  # For encryption features
bcrypt>=3.2.0  # Password hashing

# Performance
joblib>=1.1.0  # Parallel processing
asyncio  # Built-in with Python 3.7+
aiofiles>=0.8.0  # Async file operations

# Optional GPU support
# torch[cuda]  # Uncomment for CUDA support
# cupy>=9.0.0  # GPU-accelerated NumPy (optional)

# Development dependencies (install with pip install -e ".[dev]")
# These are typically specified in setup.py or pyproject.toml
